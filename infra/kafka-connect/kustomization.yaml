apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - secret.yaml
  - debezium-connect.yaml
  - debezium-connector.yaml
  - network-policy-allow-kafbat.yaml
  - s3-sink-connect.yaml
  - s3-sink-connector.yaml